// .devcontainer/devcontainer.json
{
    "name": "RapidDevEnv",
    // Build image
    "build": {
      "dockerfile": "Dockerfile",
      "args": {
        "USERNAME": "vscode",
        "USER_UID": "1000",
        "USER_GID": "1000",
        "PROJECT_NAME": "RapidDevEnv"
      }
    },
    "remoteUser": "vscode",
    "runArgs": [
        "--hostname=RapidDevEnv@host"
      // Add any additional run arguments here
    ]
}
  